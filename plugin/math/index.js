exports.run = async(client, message, input) => {
    return input
}

exports.plugin = {
    name: "math",
    type: "plugin",
    version: "1.0",
    description: "example plugin"
}
exports.config = {
    developer: true
}